# osa 2

## kurssitiedot 2

### notes:

**map**

```js
notes.map((note) => <li>{note.content}</li>);
```

(key needed)
**reduce**

```js
const sum=list.reduce(prev,current)=>prev+current, initial
```
